class Solution {
public:
    int minimumMoves(string s) {
        int count = 0;
    for(int i = 0;i<s.length();){
        if(s[i] == 'X'){
           i+=3;
            count++;
        }
        else i++;
    }
        return count;
    }
};
